export interface Dimensions {
    boxWidth: number;
    boxHeight: number;
    strokeWidth: number;
    thickBorder: number;
    color: string;
}

export const dim: Dimensions = {
    boxWidth: 400,
    boxHeight: 400,
    strokeWidth: 20,
    thickBorder: 60,
    color: 'black'
}