export interface Dimensions {
    boxWidth: number;
    boxHeight: number;
    strokeWidth: number;
    color: string;
}

export const dim: Dimensions = {
    boxWidth: 200,
    boxHeight: 200,
    strokeWidth: 20,
    color: 'black'
}