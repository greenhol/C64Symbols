export interface Dimensions {
    boxWidth: number;
    boxHeight: number;
    strokeWidth: number;
    color: string;
}

export const dim: Dimensions = {
    boxWidth: 80,
    boxHeight: 80,
    strokeWidth: 4,
    color: 'black'
}